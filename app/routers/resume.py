# from fastapi import APIRouter, HTTPException
# from app.schemas.resume_based_schema import ChatRequest, ChatResponse
# from app.memory.resume_conversation import get_session, list_sessions, reset_session
# from app.chains.resume_based_chain import get_role_conversation_chain, get_evaluation_chain
# from app.utils.logger import logger

# router = APIRouter(prefix="/chat/resume", tags=["Resume-Based-Chat"])